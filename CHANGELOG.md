# ALONG 跟课 · 版本记录

产品在用：课堂实时听写、按课型写的教练、DeepSearch、结课后的讲义。  
仓库：[github.com/Ziqi/along](https://github.com/Ziqi/along)

开课前手选 **互动 / 旁听 / 只听**。课上是听课 | 教练整屏。听课每一行下面仍有中文。翻译垫和 AI 对话已拿掉。课型上课不能改。

---

## 2026-09-09 课程脉络取代提纲

提纲是早期的权宜之计：每 12 秒调一次模型、只看最近 20 句、还顺手推一次云端，输出到后来只剩给课起个标题。现在换成按话题分段的**课程脉络**：

- 段从教练的话题流切出来（纯函数，不调模型）：话题换了、且下一拍还是新话题才算换；太短的段并入；一段最长八分钟；停写时按时间切。
- 一段结束才调一次最快模型，写这段的标题、两三句要点、老师布置的事；第一段的标题给课命名。一小时从几十次调用变成几次。
- 教练栏顶上的话题条变成脉络条：每段一个标题和开始时间，正在讲的写「现在」，点一段跳到它的卡。
- 听课栏多了「刚才讲了什么」：走神了点一下，最近几分钟浓缩成三行中文，钉在顶上，不进纪要。
- 讲义拿脉络当参考决定节的划分与顺序（仍按 2–4 节合并）；附录按段分组，印段标题、时间和要点；作业进要点。
- 段随课存在本机（`session.segments`，schema 2），刷新后还在；旧讲义里的目录字段只读不写。

---

## 2026-09-08 第二轮审查：会丢课的路堵上，讲义读完整堂课

四路代码审查（引擎 / 数据 / AI / 界面）之后的一批修复，计划、发现与待办见 `docs/review-2026-09-08.md`。

- **课不会再悄悄少一半**：屏上只留最新 180 句，以前结课时用屏上内容整段替换存档，一小时的课只剩后半；现在存档只增不减。翻译没来的句子英文照留。每 20 秒、切走标签页、离开页面都先落盘。
- **刷新后课还在**：这台设备开着的课回到「已暂停」，实录、教练卡、检索都在屏上，继续听接着同一堂。
- **本机存档不会被空壳盖掉**：课还没从盘上读出来时目录里只有空壳，以前一次读失败就把空壳写回去；现在空壳不写盘不推云，读失败提示刷新。
- **换账号不串台**：同一台电脑换人登录，上一个人的课不再被推进新账号的云端。
- **讲义读整堂课**：以前只读开头 36 句；现在头、中、尾都读，段落不再 640 字截断，中文门认真检查（英文、繁体、重复段都过不了）。
- **写好的讲义不会被草稿或改名盖掉**；两台设备各留 40 堂的规则一致。
- **卡住的检索会放手**、每个 AI 调用有时限、听写抖动一小时后仍能重连、锁屏收走麦克风会提示而不是继续送静音。
- **界面**：往回看字幕不再被顶走（「回到最新 · N 句」）；屏幕常亮；听写音质（不再混叠）；纪要入口常在，没课时是「示例讲义」；正在听的课不能从目录删；白天模式提示色对比达标；弹层在手机上限高可滚。

---

## 2026-09-08 听写掉到浏览器不再悄悄换；暂停后翻译不再被拒绝拖死

现场反馈：识别慢、听错、反复打出「M Taylor Swift」；暂停再继续听后翻译全「未译」。重复模式是浏览器自带识别器的签名，说明 xAI 听写没接上、被悄悄换掉了。最可能的原因是上一轮新加的限流按 IP 记账，一个办公室 / 教室共用一份额度，一个人续听拿不到密钥、所有人的翻译一起被拒。

- 限流按人：登录 id，没登录就是这台浏览器的 id；IP 只作 40 倍的兜底。同一个网络里三十个人各有各的额度。
- 实时听写接不上先重试一次，仍不行才换浏览器识别，并在听课栏挂「浏览器听写」和原因（太频繁 / 没钥匙 / 没接上）。点暂停再继续听会重新试实时听写。
- 浏览器识别只发新说的那半句，不再一行行重复前面的话。
- 翻译被拒「太频繁」不算一次，整队歇 8 秒再问；没钥匙一次就标「未译」，不连问三次。
- 结课过程无论出什么错都会离开「结课中」，首页和开始听不会被卡住。
- 死掉的听写连接晚到的报错和句子直接丢掉，不再干扰新接上的那条。
- 钥匙被 xAI 拒绝（没额度 / 没权限，401 / 402 / 403）时：翻译一次就标「未译」不连问三次，听课栏直接写「xAI 拒绝了这个部署的听写钥匙」。
- 新增 `scripts/ai-latency.mjs`：把服务端 `[ai]` 日志汇总成每个能力 · 模型的次数、成功率、p50 / p95、失败原因。模型和超时按这张表调。

---

## 2026-09-08 重建骨架：状态机、路由、按堂落盘、设计令牌

按全系统评审的方向做的一轮重建，产品行为不变：

- **引擎**：`use-engine.ts` 拆成显式状态机（idle → arming → listening ⇄ paused → ending → ended）和听写 / 字幕 / 教练 / 检索 / 记要点 / 我想说 / 整理七个 runtime，没有模块级全局，能单测。
- **三个面三条路由**：`/` 课堂、`/class/:id` 讲义、`/review` 复习；课在后台继续听。示例讲义只读、不进目录。
- **数据**：一堂课拆成 body 和 recap 两张表，按 `updated_at` 游标增量同步，更新者胜，墓碑跨设备删课；本机以 IndexedDB 按堂存，localStorage 只留索引；40 堂 / 512 KB / 256 KB 三个上限两端共用。
- **AI 入口**：每个服务函数过同源校验 + 调用者识别 + 每人每分钟令牌桶；模型名、提示词、错误码各只在一处。
- **界面**：自托管 IBM Plex；五档字阶三种行高；四级按钮固定尺寸；一种划词；「记要点」是上课时最重的键，结课降为次级加确认；纪要页工具三组；新的弹层 / 确认 / 菜单三件；「检索」代替界面上的 DeepSearch。
- **复习**：会了 / 再来真的分流（Leitner 盒子，1/3/7/14/30 天到期），只存本机。**「我想说」**：一句中文出一句这堂能开口的英文，记入纪要。

---

## 2026-09-08 讲义门：教练概括也算，正文两次就停

没点检索、没记笔记时，正文也必须能对上至少一张教练卡的概括（题目 / brief）。三条开口仍不是原料。

点一次整理，正文最多两轮：先并行写一轮，不够再收一轮。还不行就停，人再点「整理本堂」。不再后台连打第三轮。

---

## 2026-09-08 讲义收口，复习先过这一堂

课上这条线已经立住。这一步不再往上课堆按钮，而是把「这一堂 → 带走的讲义 → 还能复习」收成一条线。

结课讲义现在和教练用同一套装配：形状不对就重写，不编假段落。

- **正文必须像文章。** 导语 + 2～4 段主题。听写残句不当标题，只有 1. 2. 3. 的目录不当完稿。
- **课上材料进正文，不当抄袭。** 教练卡、DeepSearch、随手记是原料。点过的名字和数字写进对应段。三条开口仍只在附录。
- **课型写进语气。** 互动 / 旁听写这一小时的张力；只听写成听后笔记，不要假装对老师说过话。
- **语言点必须能指回这一堂。** 对不上实录 / 教练 / 检索 / 随手记的词条丢掉。复习默认先抽本堂，英文正面，中文和用法在背面。跨课是第二层。

上课 HUD 维持现状。

---

## 2026-09-08 审查后补的几处

只听的「这句」本来就可以是刚听到的那句，装配器不能当复读丢掉。没接到模型不要自动再写一遍。However / Whoever 不当问句。教练栏的「暂停」改成「停写」，免得和顶栏停麦搅在一起。已经听到字幕、卡还没来时，右边写「已经在听」，不再像还没开始。

接着又补：点停写要真的丢掉这一轮，不能过一会儿还弹出一张卡。没写出来时，停写不能把「重写」盖掉。没卡时不给 DeepSearch。教练栏不再叠一层「记要点」，记要点仍在顶栏。已经开课、字幕还没来时，听课栏写「已经在听」，不再说「点开始听」。

再补前端指出的几处：DeepSearch 只留在卡上。停写后失败仍可重写，但会说明跟听已停。互动例子里带着刚听到的半句不再整张失败。没标题用上一张或「这一拍」，不用听写碎片。点重写若三条一样，不再挂第二张。剖析 / 背景中文按句裁，不再 40 字腰斩。延展 / 追深仍可空，不编假句。

---

## 2026-09-07 三种课型的教练装配器

「连不上」不是旁听写错了。教练没接到模型（这台环境没有钥匙，或接口没回）才会这么说。慢了是超时。没三条是模型回了但装配不成形。

教练现在有装配器，只收口形状，不编开口：

- **互动 / 旁听**：讨论三条是同意、对比、例子；问句三条是直接答、补一层、举个例。旁听只是写成「若要开口」，人多时少刷。
- **只听**：这句、剖析、背景。扩展两条丢掉，不当成对老师说的话。
- 少一条就失败，不会补一句假的。复读刚听到的那句也丢掉。

先 `grok-4.6`（low），不够再退最快聊天模型。装配测试盖三种课型。

---

## 2026-09-07 旁听右边空着、只说在重写

选旁听后，右边会一直空，顶栏却写「这轮慢了，正在重写」。其实第一遍超时就被掐掉了，重写也还没开始，面板又退回那句说明。

- 第一遍写不完会接着用更快的模型再写一遍，外面留够时间，不再 10 秒就掐死。
- 正在写、正在重写、没写出来，三种状态分开说。没写出来才出现「重写」，点了才会再写。
- 写成功了才清掉上一句错误；听着课却还没挂上这一堂时，先挂上再出卡，不再默默丢掉。

---

## 2026-09-07 课型锁定，界面更好读

课型只在「开始听」时选一次。上课顶栏只写「这堂是××」，点不了。想换：先结课，再开一堂。

- 清掉已经不用的对话、翻译垫代码。Esc 只关记要点。
- 只听三块拉开：这句像摘录，剖析像批注，背景像小注。不再是缩小版 1.2.3 开口。
- 听课英文更大；中文用 Noto Sans SC，英文继续用无衬线。
- 顶栏主次：听课中、记要点、纪要、结课、暂停更显眼；登录和夜间靠右变淡。
- 纪要目录带日期和课型；复习和整理、导出同一排；章节名改成能读的中文。课堂随手记默认展开，旁标注「可补」。

---

## 2026-09-07 三种课型，教练跟着变

开课前手选这堂怎么听，写进这一堂。继续听不再问。顶栏小字可中途改，下一张卡按新课型写。

- **互动**：讨论给同意 / 对比 / 例子；问句给直接答 / 补一层 / 举个例。
- **旁听**：同一套开口，写成「若要开口」，人多时少刷。
- **只听**：这句 + 剖析 + 背景。不再假装对老师说话。检索改成背景和资料。

桌面听课 | 教练整屏并排。手机只留这两个页签。听课行下的中文翻译留下。纪要 Part 3：互动 / 旁听仍是开口原件，只听改成课中剖析。

跟听按新句子走，不是每 12 秒换主题。上一轮还在写时只记住此刻最后一句。卡住才用最后一句再写。

---

## 2026-09-07 课上跟听与纪要 · 现场修补

医院那堂（中国医院 vs 菲律宾等候）能马上用同意 / 对比 / 例子，但中途「译…」卡死、教练停更；暂停再继续才好。开口原件像被后一张盖掉。纪要底下空「要点」其实是没写过的随手记。

### 翻译停在「译…」，教练不再出卡

**问题**  
听写还在进，字幕一直「译…」，教练也不出新卡。根因：翻译超时叠死、教练只追旧句、上一轮没写完就把后面的句子丢掉、切走纪要再回来听写不续。

**处理**  
翻译只追最新几句，超时放开。教练上一张还在写时只记住此刻最后一句，写完再用这一句。卡住（超时 / 报错 / 停着）才重写，不是 12 秒换主题。切回课页会续上听写。界面写「跟听中 / 这轮慢了，正在重写」，有新句子时不假「待命」。

审查后又补：模型回了「同一张卡」也算跟上了，不再空转打教练。

### 开口原件像被盖掉

**问题**  
没点 DeepSearch，同一主题两张卡标题一样，新卡把人拽到最底，旧的像没了。

**处理**  
正在看旧卡、或列表不在最底，新卡不准抢焦点。同一主题两拍写成 `等候时间 · 2`。DeepSearch 仍按卡 id，不按主题名互盖。

### 空「要点」和补不了的随手记

**问题**  
纪要底下「要点」是空的课堂随手记，不是讲义 Takeaways。后来整块被藏掉，没在课上点过「记」的人，结课后补不了一句。

**处理**  
有记下的才叫「课堂随手记」。空堂也留着这一块，可以自己补。

### 「手写课堂上的一句英文」

**问题**  
听课栏底下那行看起来像主功能。打进去会当成又听到的一句，和麦克风抢同一条听写，教练也按这句写。

**处理**  
拿掉。左边只收麦克风落下的字幕。

---

## 2026-09-07 讲义两段写、整页出版物

- 整理本堂先写导语和章节并铺上纸，再写语言点。进度是 1/2、2/2，不再假装百分比。
- 课表收进「目录」。正文是一张居中的讲义纸。要点折进 Notes。

---

## 2026-09-07 教练不再因 same 丢卡

- `same=true` 只表示沿用上一个主题名，不再把已经写好的 3+2 扔掉。
- 问句一律新开一张。不再因为主题名碰巧相同就判同一拍。
- 只有同一句 12 秒内重跑、或三条英文完全一样，才跳过。

---

## 2026-09-07 讲义阅读态 · DeepSearch 按卡

- 纪要默认是一张讲义纸：整理 / 导出收进菜单，登录提示离开正文。打印时藏跟课顶栏和课表。
- DeepSearch 按教练卡 id 存。同名主题不再互盖；旧课按主题键补到卡 id。
- 正在看某张卡或该卡 DeepSearch 时，新题不抢焦点、不把正文滚走。

---

## 2026-09-07 DeepSearch 先检索再写

- 「Flash」是代码绰号，指最快聊天模型 `grok-4.20-0309-non-reasoning`，不是另一家产品。
- 教练仍用 grok-4.6。DeepSearch 检索改用最快模型 + 网页搜索；有事实后 4.6 只根据这些事实写四十秒发言，不再联网。
- 搜不到就报「没检索到」，不许用编出来的段落充完稿。

---

## 2026-09-07 Cursor · SpaceX 讲义

现场卡死点：`recapClass` 对「Long-Term Vision vs Quarterly Pressure」交不出导语和段落。失败不再拿目录充数之后，只剩红字。

这一提交：

- **先写出这一堂**：导语（季报压力 vs 5–10 年 horizon）、四节正文、词表（quarterly earnings / time horizon / payoff / retail investor，每条有用法和中文）、划线与词表一致；教练 3+2 和 DeepSearch 只在附录。标题不带 `· 再出`。
- 打开这堂课就能读到讲义。本机那堂若还是空稿 / 只有目录，hydrate 时补上同一份正文。
- `recapClass`：把 12s / 1800 token 的截断改掉——Flash 与 grok-4.6 并行写内容（约 20–28s，4000 token），不够再让 grok-4.6 写三节；JSON 截断会捞已经写完的 section。不再用词频表充单词。
- 装配器不再把无正文的 heading 提成 section。
- 这一堂模型仍写不出来时，用写好的讲义收口，不再交白卷。

---

## 2026-09-07 晚

现场：SpaceX 那堂「Long-Term Vision vs Quarterly Pressure」再出一份仍是目录；整理本堂连续四次红字「纪要没写出来」。

这一提交：

- 纪要定义改成**讲义**：模型读实录 + 笔记 + 教练 + DeepSearch，再写内容/语言；教练原件只做附录。
- 选词、难词、划线交给模型，拿掉词频和后缀规则。
- `isFilled`：没有导语和段落不算完稿。有教练卡也不能把目录存成完成稿。
- 失败就报错并提示「整理本堂」，不再渲染 Contents/Map 充数。
- fork 标题不再追加 `· 再出`。

**仍未过关（交给 Cursor）：** `recapClass` 对这堂课写不出合格讲义。见 [CURSOR.md](./CURSOR.md) §6 P0。

---

## 2026-09-07

这一版把「目录当完稿、DeepSearch 卡死、手机对不上电脑」这几件钉住。下面是现场出过的问题和处理。

### 手机和电脑纪要对不上

**问题**  
手机上看不到登录。纪要只写在那台浏览器的缓存里，换设备就是另一套。

**处理**  
登录本来就有（Google / X），云端按账号存。手机顶栏和「更多」里都露出「登录」。纪要页未登录会提示：登录后手机和电脑同步。登录后自动把本机和云端合并。

没登录：只在这台设备。清缓存会空。  
登录后：同一账号，手机和电脑是同一份。

### 纪要只有目录，没有正文

**问题**  
结课后只剩 Contents + Map。模型交了提纲，装配器当完稿，按钮变成「再出一份」。中英对不上、单词表是 `think / like / really`，听写碎片当正文。

**处理**  
- 装配器不把听写和词频当完稿。没有段落不算写完。  
- 目录会扩成每个主题下的段落。先导语和正文，目录不再当全文。  
- 结课把**教练卡和 DeepSearch** 装进纪要第三块，不靠模型重写。漏了的主题用课上那张卡补。  
- 模型对着主题写正文；快模型和 4.6 一起写，失败自动再写一次。  
- 实录先去重。中文必须翻译同一行英文。

纪要结构：

1. 课堂内容（导语、要点、每个主题的段落）  
2. 英语学习（单词、搭配、句式、语法、例句）  
3. 教练（三条开口、两条延展；点过 DeepSearch 的检索稿跟在下面）

### DeepSearch 要两分钟，连点会卡死

**问题**  
检索串着 4.6，一路拖到一分钟以上。全局一把锁，点第二张把第一张掐掉。

**处理**  
快写和联网检索同时跑，大约 10 秒出稿。检索超时用已经写好的。每张卡各算各的，最多两路同时，第三路排队。简体中文。不再用教练的 1.2.3 冒充深稿。

### 听写冒出 `???????`，翻译停在「译…」

**问题**  
噪声被当成一句话，还拿去译。

**处理**  
没有字母的、纯问号、纯标点、`uh/um` 直接丢掉，不进实录、不译、不喂教练。

### 其它这一轮收掉的

- 结课后教练还挂在首页：结课中止在途的教练。  
- 置顶把别的课推进「收藏」：只保留置顶，新课默认不置顶。  
- 教练同一主题覆盖旧卡：问句新开一张；DeepSearch 跟主题走。  
- 暂停听课没真停、翻译切走纪要会丢：听写和翻译分路，暂停清队列。  
- 标题被切成 `Universal Ba · 0C`：标题可折行。

---

## 模型（当前）

| 能力 | 模型 |
|---|---|
| 听写 | xAI 流式听写。英文字幕不经过 4.6。 |
| 字幕翻译、脉络段收口、刚才讲了什么 | `grok-4.20-0309-non-reasoning`（绰号 Flash） |
| 教练 | `grok-4.6` low，超时退最快聊天模型 |
| 检索（DeepSearch） | 最快模型检索；4.6 只根据事实写发言 |
| 纪要正文 | 最快模型与 grok-4.6 并行 |

---

## 更早
